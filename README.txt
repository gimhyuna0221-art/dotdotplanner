DotDotPlanner Future Log patch

Replace only these two files in your existing project folder:
- app.js
- index.html

Changes:
1. Future Log date labels no longer overlap the type icon/title.
2. Future Log mini-calendar default scale is now the maximum 140%.
3. Existing Ctrl+mouse-wheel resizing remains active (80% to 140%), with responsive container caps on narrow cards.
4. Legacy saved 100% default migrates to the new 140% default; explicitly chosen 80-135% values are preserved.

After replacing the files, refresh the browser with Ctrl+F5.
