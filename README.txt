DotDotPlanner Future Log date label readability update

Changes:
- Future Log short date labels: 9.5px -> 11.5px
- Compact/long-range date labels: 8.5px -> 10px
- Existing 48px date column, overlap prevention, 140% mini-calendar default, and responsive scaling are unchanged.

Apply:
Copy app.js and index.html into your existing dotdotplanner_coordinate_lock_v21 folder and overwrite files with the same names.
Then refresh the browser with Ctrl+F5.
