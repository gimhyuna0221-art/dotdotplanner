# DotDotPlanner 작업 규칙

## 현재 단계

현재는 기능 구현 전, 정적 디자인 정밀 고정 단계다.
index.html은 승인된 디자인 기준본이다.

## 우선순위

1. 사용자가 현재 메시지에서 지정한 수정 범위
2. 첨부된 목표 레퍼런스
3. 기존 index.html의 나머지 영역 보존
4. UI/UX Pro Max의 검수 규칙
5. 그 밖의 일반적인 디자인 권고

일반적인 디자인 권고가 사용자의 지시나 레퍼런스와 충돌하면 적용하지 않는다.

## 수정 규칙

- 한 번에 한 영역만 수정한다.
- 사용자에게 허용받은 CSS 선택자만 수정한다.
- 수정 전에 차이와 변경 예정 값을 먼저 보고한다.
- 사용자 승인 전에는 파일을 수정하지 않는다.
- HTML 구조를 변경하지 않는다.
- JavaScript나 기능을 추가하지 않는다.
- 지정하지 않은 서체, 색상, 크기, 좌표를 변경하지 않는다.
- transform: scale로 구성 요소 전체를 축소하거나 확대하지 않는다.
- 기존 파일을 덮어쓸 때는 수정 전 상태를 보존한다.

## 현재 확정 상태

- 달력 영역은 수정 완료 상태다.
- 데일리 영역도 명시적인 허가 없이는 수정하지 않는다.
- 다음 작업 대상은 Weekly 영역이다.
